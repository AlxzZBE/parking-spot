spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parking-control-db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

server:
  error:
    include-stacktrace: on_param